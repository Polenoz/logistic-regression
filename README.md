[![Open in Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/Polenoz/logistic-regression/blob/main/Logistische_regression.ipynb)

# Logistic Regression Project

In this project, a logistic regression model is built using Python and scikit-learn. The goal is to classify data based on the Advertising dataset and evaluate how well the model performs.

## Dataset

The project uses the file:

- Advertising.csv

The dataset is loaded directly from the GitHub repository, so no manual upload is needed when the notebook is opened in Google Colab.

## Required libraries

This project uses the following libraries:

- pandas
- numpy
- matplotlib
- seaborn
- scikit-learn
- jupyter

## How to run

The easiest way is to use Google Colab:

- Click the badge at the top of this README
- Open the notebook in Colab
- Run all cells from top to bottom

The dataset is loaded automatically from the repository.

You can also run the notebook locally:

- Clone or download the repository
- Install the required libraries
- Open the notebook with Jupyter
- Run all cells

Example installation:

```bash
pip install pandas numpy matplotlib seaborn scikit-learn jupyter
